{
  "compilerOptions": {
    "target": "ES6",                        // Specifies the target JavaScript version.
    "module": "commonjs",                    // Specifies the module system to use (CommonJS, ES6, etc.).
    "strict": true,                          // Enables all strict type-checking options.
    "esModuleInterop": true,                 // Allows default importing of modules without using `import * as`.
    "skipLibCheck": true,                    // Skips type checking of declaration files (.d.ts).
    "outDir": "./dist",                      // Specifies the output directory for the compiled files.
    "rootDir": "./src",                      // Specifies the root directory of your TypeScript source files.
    "resolveJsonModule": true,               // Allows importing `.json` files as modules.
    "allowJs": true,                         // Allows JavaScript files to be compiled.
    "forceConsistentCasingInFileNames": true // Enforces case-sensitivity when referencing files.
  },
  "include": [
    "src/**/*"                               // Includes all files in the "src" directory.
  ],
  "exclude": [
    "node_modules"                           // Excludes the "node_modules" folder.
  ]
}
